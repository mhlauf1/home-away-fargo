// Canonical production origin. Vercel redirects the apex domain to www,
// so all absolute URLs (sitemap, canonicals, structured data) must use www.
export const SITE_URL = 'https://www.homeawayfargo.com'
